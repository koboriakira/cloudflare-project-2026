# docs/contexts

このディレクトリは、ドメイン境界（bounded context）とコンテキストマップを置く場所である。プロジェクトが複数のサブドメインにまたがり、各コンテキストの責務・境界・他コンテキストとの関係（何を共有し、何を共有しないか）を明文化する必要が生じたら、ここにコンテキストごとのドキュメントを追加する。テンプレートの状態ではプレースホルダーであり、単一コンテキストの小規模プロジェクトでは空のままでよい。
